import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// State notes
// Change the state, there is a method called set state

// Template
// Western confrence toggle
// display rankings, otherwise display error